import Input from "@/components/Input";

const SignInForm = () => {
  return (
    <div className="w-full">
      <Input placeholder="이메일" label={"이메일"} />
    </div>
  );
};

export default SignInForm;
